import styled from 'styled-components';

export const Wrapper = styled.option`
display: flex;
gap: 10px;

.checkbox-size {
  width: 24px;
  height: 24px;
}

.option-text {
  font-size: 16px;
  line-height: 28px;
  letter-spacing: 0.75px;
  font-weight: 400;
}
`;
